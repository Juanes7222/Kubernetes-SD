apiVersion: v1
kind: Service
metadata:
  name: tasks-service
  namespace: microservices-app
  labels:
    app: tasks-service
spec:
  selector:
    app: tasks-service
  ports:
  - name: http
    protocol: TCP
    port: 8001
    targetPort: 8001
  type: ClusterIP
